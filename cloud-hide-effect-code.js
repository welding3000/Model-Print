### ::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::: ###
###  Cloud-Print-Output [  Hide --- Code --- Effect  ]###
### ::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::: ###

########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  0 ] ###

$ ->
  state = true
  $('#hide_print_button_code ').on 'click', ->
    if state
      $('#hide_show_print_code').show 2000
    else
      $('#hide_show_print_code').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  1 ] ###

$ ->
  state = true
  $('#hide_print_button_code1 ').on 'click', ->
    if state
      $('#hide_show_print_code1').show 2000
    else
      $('#hide_show_print_code1').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  2 ] ###

$ ->
  state = true
  $('#hide_print_button_code2 ').on 'click', ->
    if state
      $('#hide_show_print_code2').show 2000
    else
      $('#hide_show_print_code2').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  3 ] ###

$ ->
  state = true
  $('#hide_print_button_code3 ').on 'click', ->
    if state
      $('#hide_show_print_code3').show 2000
    else
      $('#hide_show_print_code3').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  4 ] ###

$ ->
  state = true
  $('#hide_print_button_code4 ').on 'click', ->
    if state
      $('#hide_show_print_code4').show 2000
    else
      $('#hide_show_print_code4').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  5 ] ###

$ ->
  state = true
  $('#hide_print_button_code5 ').on 'click', ->
    if state
      $('#hide_show_print_code5').show 2000
    else
      $('#hide_show_print_code5').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  6 ] ###

$ ->
  state = true
  $('#hide_print_button_code6 ').on 'click', ->
    if state
      $('#hide_show_print_code6').show 2000
    else
      $('#hide_show_print_code6').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  7 ] ###

$ ->
  state = true
  $('#hide_print_button_code7 ').on 'click', ->
    if state
      $('#hide_show_print_code7').show 2000
    else
      $('#hide_show_print_code7').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  8 ] ###

$ ->
  state = true
  $('#hide_print_button_code8 ').on 'click', ->
    if state
      $('#hide_show_print_code8').show 2000
    else
      $('#hide_show_print_code8').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  9 ] ###

$ ->
  state = true
  $('#hide_print_button_code9 ').on 'click', ->
    if state
      $('#hide_show_print_code9').show 2000
    else
      $('#hide_show_print_code9').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  10 ] ###

$ ->
  state = true
  $('#hide_print_button_code10 ').on 'click', ->
    if state
      $('#hide_show_print_code10').show 2000
    else
      $('#hide_show_print_code10').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  11 ] ###

$ ->
  state = true
  $('#hide_print_button_code11 ').on 'click', ->
    if state
      $('#hide_show_print_code11').show 2000
    else
      $('#hide_show_print_code11').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  12 ] ###

$ ->
  state = true
  $('#hide_print_button_code12 ').on 'click', ->
    if state
      $('#hide_show_print_code12').show 2000
    else
      $('#hide_show_print_code12').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  13 ] ###

$ ->
  state = true
  $('#hide_print_button_code13 ').on 'click', ->
    if state
      $('#hide_show_print_code13').show 2000
    else
      $('#hide_show_print_code13').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  14 ] ###

$ ->
  state = true
  $('#hide_print_button_code14 ').on 'click', ->
    if state
      $('#hide_show_print_code14').show 2000
    else
      $('#hide_show_print_code14').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  15 ] ###

$ ->
  state = true
  $('#hide_print_button_code15 ').on 'click', ->
    if state
      $('#hide_show_print_code15').show 2000
    else
      $('#hide_show_print_code15').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  16 ] ###

$ ->
  state = true
  $('#hide_print_button_code16 ').on 'click', ->
    if state
      $('#hide_show_print_code16').show 2000
    else
      $('#hide_show_print_code16').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number --- 17 ] ###

$ ->
  state = true
  $('#hide_print_button_code17 ').on 'click', ->
    if state
      $('#hide_show_print_code17').show 2000
    else
      $('#hide_show_print_code17').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  18 ] ###

$ ->
  state = true
  $('#hide_print_button_code18 ').on 'click', ->
    if state
      $('#hide_show_print_code18').show 2000
    else
      $('#hide_show_print_code18').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  19 ] ###

$ ->
  state = true
  $('#hide_print_button_code19 ').on 'click', ->
    if state
      $('#hide_show_print_code19').show 2000
    else
      $('#hide_show_print_code19').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  20 ] ###

$ ->
  state = true
  $('#hide_print_button_code20 ').on 'click', ->
    if state
      $('#hide_show_print_code20').show 2000
    else
      $('#hide_show_print_code20').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  21 ] ###

$ ->
  state = true
  $('#hide_print_button_code21 ').on 'click', ->
    if state
      $('#hide_show_print_code21').show 2000
    else
      $('#hide_show_print_code21').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  22 ] ###

$ ->
  state = true
  $('#hide_print_button_code22 ').on 'click', ->
    if state
      $('#hide_show_print_code22').show 2000
    else
      $('#hide_show_print_code22').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  23 ] ###

$ ->
  state = true
  $('#hide_print_button_code23 ').on 'click', ->
    if state
      $('#hide_show_print_code23').show 2000
    else
      $('#hide_show_print_code23').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  24 ] ###

$ ->
  state = true
  $('#hide_print_button_code24 ').on 'click', ->
    if state
      $('#hide_show_print_code24').show 2000
    else
      $('#hide_show_print_code24').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  25 ] ###

$ ->
  state = true
  $('#hide_print_button_code25 ').on 'click', ->
    if state
      $('#hide_show_print_code25').show 2000
    else
      $('#hide_show_print_code25').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  26 ] ###

$ ->
  state = true
  $('#hide_print_button_code26 ').on 'click', ->
    if state
      $('#hide_show_print_code26').show 2000
    else
      $('#hide_show_print_code26').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########

########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  27 ] ###

$ ->
  state = true
  $('#hide_print_button_code27 ').on 'click', ->
    if state
      $('#hide_show_print_code27').show 2000
    else
      $('#hide_show_print_code27').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  28 ] ###

$ ->
  state = true
  $('#hide_print_button_code28 ').on 'click', ->
    if state
      $('#hide_show_print_code28').show 2000
    else
      $('#hide_show_print_code28').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  29 ] ###

$ ->
  state = true
  $('#hide_print_button_code29 ').on 'click', ->
    if state
      $('#hide_show_print_code29').show 2000
    else
      $('#hide_show_print_code29').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  30 ] ###

$ ->
  state = true
  $('#hide_print_button_code30 ').on 'click', ->
    if state
      $('#hide_show_print_code30').show 2000
    else
      $('#hide_show_print_code30').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  31 ] ###

$ ->
  state = true
  $('#hide_print_button_code31 ').on 'click', ->
    if state
      $('#hide_show_print_code31').show 2000
    else
      $('#hide_show_print_code31').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  32 ] ###

$ ->
  state = true
  $('#hide_print_button_code32 ').on 'click', ->
    if state
      $('#hide_show_print_code32').show 2000
    else
      $('#hide_show_print_code32').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  33 ] ###

$ ->
  state = true
  $('#hide_print_button_code33 ').on 'click', ->
    if state
      $('#hide_show_print_code33').show 2000
    else
      $('#hide_show_print_code33').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  34 ] ###

$ ->
  state = true
  $('#hide_print_button_code34 ').on 'click', ->
    if state
      $('#hide_show_print_code34').show 2000
    else
      $('#hide_show_print_code34').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  35 ] ###

$ ->
  state = true
  $('#hide_print_button_code35 ').on 'click', ->
    if state
      $('#hide_show_print_code35').show 2000
    else
      $('#hide_show_print_code35').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  36 ] ###

$ ->
  state = true
  $('#hide_print_button_code36 ').on 'click', ->
    if state
      $('#hide_show_print_code36').show 2000
    else
      $('#hide_show_print_code36').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  37 ] ###

$ ->
  state = true
  $('#hide_print_button_code37 ').on 'click', ->
    if state
      $('#hide_show_print_code37').show 2000
    else
      $('#hide_show_print_code37').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  38 ] ###

$ ->
  state = true
  $('#hide_print_button_code38 ').on 'click', ->
    if state
      $('#hide_show_print_code38').show 2000
    else
      $('#hide_show_print_code38').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  39 ] ###

$ ->
  state = true
  $('#hide_print_button_code39 ').on 'click', ->
    if state
      $('#hide_show_print_code39').show 2000
    else
      $('#hide_show_print_code39').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  40 ] ###

$ ->
  state = true
  $('#hide_print_button_code40 ').on 'click', ->
    if state
      $('#hide_show_print_code40').show 2000
    else
      $('#hide_show_print_code40').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  41 ] ###

$ ->
  state = true
  $('#hide_print_button_code41 ').on 'click', ->
    if state
      $('#hide_show_print_code41').show 2000
    else
      $('#hide_show_print_code41').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  42 ] ###

$ ->
  state = true
  $('#hide_print_button_code42 ').on 'click', ->
    if state
      $('#hide_show_print_code42').show 2000
    else
      $('#hide_show_print_code42').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  43 ] ###

$ ->
  state = true
  $('#hide_print_button_code43 ').on 'click', ->
    if state
      $('#hide_show_print_code43').show 2000
    else
      $('#hide_show_print_code43').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  44 ] ###

$ ->
  state = true
  $('#hide_print_button_code44 ').on 'click', ->
    if state
      $('#hide_show_print_code44').show 2000
    else
      $('#hide_show_print_code44').hide 2000
    state = !state
    return
  return

########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  45 ] ###

$ ->
  state = true
  $('#hide_print_button_code45 ').on 'click', ->
    if state
      $('#hide_show_print_code45').show 2000
    else
      $('#hide_show_print_code45').hide 2000
    state = !state
    return
  return

########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  46 ] ###

$ ->
  state = true
  $('#hide_print_button_code46 ').on 'click', ->
    if state
      $('#hide_show_print_code46').show 2000
    else
      $('#hide_show_print_code46').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  47 ] ###

$ ->
  state = true
  $('#hide_print_button_code47 ').on 'click', ->
    if state
      $('#hide_show_print_code47').show 2000
    else
      $('#hide_show_print_code47').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  48 ] ###

$ ->
  state = true
  $('#hide_print_button_code48 ').on 'click', ->
    if state
      $('#hide_show_print_code48').show 2000
    else
      $('#hide_show_print_code48').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  49 ] ###

$ ->
  state = true
  $('#hide_print_button_code49 ').on 'click', ->
    if state
      $('#hide_show_print_code49').show 2000
    else
      $('#hide_show_print_code49').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########

########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  50 ] ###

$ ->
  state = true
  $('#hide_print_button_code50 ').on 'click', ->
    if state
      $('#hide_show_print_code50').show 2000
    else
      $('#hide_show_print_code50').hide 2000
    state = !state
    return
  return

########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  51 ] ###

$ ->
  state = true
  $('#hide_print_button_code51 ').on 'click', ->
    if state
      $('#hide_show_print_code51').show 2000
    else
      $('#hide_show_print_code51').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  52 ] ###

$ ->
  state = true
  $('#hide_print_button_code52 ').on 'click', ->
    if state
      $('#hide_show_print_code52').show 2000
    else
      $('#hide_show_print_code52').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  53 ] ###

$ ->
  state = true
  $('#hide_print_button_code53 ').on 'click', ->
    if state
      $('#hide_show_print_code53').show 2000
    else
      $('#hide_show_print_code53').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  54 ] ###

$ ->
  state = true
  $('#hide_print_button_code54 ').on 'click', ->
    if state
      $('#hide_show_print_code54').show 2000
    else
      $('#hide_show_print_code54').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  55 ] ###

$ ->
  state = true
  $('#hide_print_button_code55 ').on 'click', ->
    if state
      $('#hide_show_print_code55').show 2000
    else
      $('#hide_show_print_code55').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  56 ] ###

$ ->
  state = true
  $('#hide_print_button_code56 ').on 'click', ->
    if state
      $('#hide_show_print_code56').show 2000
    else
      $('#hide_show_print_code56').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  57 ] ###

$ ->
  state = true
  $('#hide_print_button_code57 ').on 'click', ->
    if state
      $('#hide_show_print_code57').show 2000
    else
      $('#hide_show_print_code57').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  58 ] ###

$ ->
  state = true
  $('#hide_print_button_code58 ').on 'click', ->
    if state
      $('#hide_show_print_code58').show 2000
    else
      $('#hide_show_print_code58').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  59 ] ###

$ ->
  state = true
  $('#hide_print_button_code59 ').on 'click', ->
    if state
      $('#hide_show_print_code59').show 2000
    else
      $('#hide_show_print_code59').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########    
  
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  60 ] ###

$ ->
  state = true
  $('#hide_print_button_code60 ').on 'click', ->
    if state
      $('#hide_show_print_code60').show 2000
    else
      $('#hide_show_print_code60').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  61 ] ###

$ ->
  state = true
  $('#hide_print_button_code61 ').on 'click', ->
    if state
      $('#hide_show_print_code61').show 2000
    else
      $('#hide_show_print_code61').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  62 ] ###

$ ->
  state = true
  $('#hide_print_button_code62 ').on 'click', ->
    if state
      $('#hide_show_print_code62').show 2000
    else
      $('#hide_show_print_code62').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  63 ] ###

$ ->
  state = true
  $('#hide_print_button_code63 ').on 'click', ->
    if state
      $('#hide_show_print_code63').show 2000
    else
      $('#hide_show_print_code63').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  64 ] ###

$ ->
  state = true
  $('#hide_print_button_code64 ').on 'click', ->
    if state
      $('#hide_show_print_code64').show 2000
    else
      $('#hide_show_print_code64').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  65 ] ###

$ ->
  state = true
  $('#hide_print_button_code65 ').on 'click', ->
    if state
      $('#hide_show_print_code65').show 2000
    else
      $('#hide_show_print_code65').hide 2000
    state = !state
    return
  return
###


########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  66 ] ###

$ ->
  state = true
  $('#hide_print_button_code66 ').on 'click', ->
    if state
      $('#hide_show_print_code66').show 2000
    else
      $('#hide_show_print_code66').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  67 ] ###

$ ->
  state = true
  $('#hide_print_button_code67 ').on 'click', ->
    if state
      $('#hide_show_print_code67').show 2000
    else
      $('#hide_show_print_code67').hide 2000
    state = !state
    return
  return

########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  68 ] ###

$ ->
  state = true
  $('#hide_print_button_code68 ').on 'click', ->
    if state
      $('#hide_show_print_code68').show 2000
    else
      $('#hide_show_print_code68').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  69 ] ###

$ ->
  state = true
  $('#hide_print_button_code69 ').on 'click', ->
    if state
      $('#hide_show_print_code69').show 2000
    else
      $('#hide_show_print_code69').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########    

########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  70 ] ###

$ ->
  state = true
  $('#hide_print_button_code70 ').on 'click', ->
    if state
      $('#hide_show_print_code70').show 2000
    else
      $('#hide_show_print_code70').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  71 ] ###

$ ->
  state = true
  $('#hide_print_button_code71 ').on 'click', ->
    if state
      $('#hide_show_print_code71').show 2000
    else
      $('#hide_show_print_code71').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  72 ] ###

$ ->
  state = true
  $('#hide_print_button_code72 ').on 'click', ->
    if state
      $('#hide_show_print_code72').show 2000
    else
      $('#hide_show_print_code72').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  73 ] ###

$ ->
  state = true
  $('#hide_print_button_code73 ').on 'click', ->
    if state
      $('#hide_show_print_code73').show 2000
    else
      $('#hide_show_print_code73').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  74 ] ###

$ ->
  state = true
  $('#hide_print_button_code74 ').on 'click', ->
    if state
      $('#hide_show_print_code74').show 2000
    else
      $('#hide_show_print_code74').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  75 ] ###

$ ->
  state = true
  $('#hide_print_button_code75 ').on 'click', ->
    if state
      $('#hide_show_print_code75').show 2000
    else
      $('#hide_show_print_code75').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  76 ] ###

$ ->
  state = true
  $('#hide_print_button_code76 ').on 'click', ->
    if state
      $('#hide_show_print_code76').show 2000
    else
      $('#hide_show_print_code76').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  77 ] ###

$ ->
  state = true
  $('#hide_print_button_code77 ').on 'click', ->
    if state
      $('#hide_show_print_code77').show 2000
    else
      $('#hide_show_print_code77').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
  
  
  
  
  
  
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  77 ] ###

$ ->
  state = true
  $('#hide_print_button_code77 ').on 'click', ->
    if state
      $('#hide_show_print_code77').show 2000
    else
      $('#hide_show_print_code77').hide 2000
    state = !state
    return
  return
###
    
    
    
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  78 ] ###

$ ->
  state = true
  $('#hide_print_button_code78 ').on 'click', ->
    if state
      $('#hide_show_print_code78').show 2000
    else
      $('#hide_show_print_code78').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  79 ] ###

$ ->
  state = true
  $('#hide_print_button_code79 ').on 'click', ->
    if state
      $('#hide_show_print_code79').show 2000
    else
      $('#hide_show_print_code79').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########    

########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  80 ] ###

$ ->
  state = true
  $('#hide_print_button_code80 ').on 'click', ->
    if state
      $('#hide_show_print_code80').show 2000
    else
      $('#hide_show_print_code80').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########
### NUMBER-Cloud-Output  [ Number ---  81 ] ###

$ ->
  state = true
  $('#hide_print_button_code81 ').on 'click', ->
    if state
      $('#hide_show_print_code81').show 2000
    else
      $('#hide_show_print_code81').hide 2000
    state = !state
    return
  return
###
########--------------[   ]-----------------------########    
      
